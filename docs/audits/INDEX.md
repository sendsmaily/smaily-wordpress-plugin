# Audits — register

**Single index of every audit run on Smaily Connect.** This folder
(`docs/audits/`) holds the audit reports; this file is the table that says what
each one covered, when, against which repo state, and what it concluded. New
audits get a row here **and** a report file in this folder.

> Why this folder exists: audits were scattered in `docs/`. Keeping them together
> with one register makes "when did we last look at X, and has the code moved
> since?" answerable at a glance — and enforces the re-audit policy below.

---

## Register

| Audit | Date | Repo state (baseline) | Scope | Auditor | Outcome | Report |
|---|---|---|---|---|---|---|
| **Codebase audit (Fable)** | 2026-06-11 | `906cf3d` · v2.0.0-beta.1 | Whole codebase — functionality / security / DB-quality (3 parallel passes) | Claude (Fable 5) | 1 CRITICAL (password in debug.log → F1) + 1 IMPORTANT (static crypto IV → F3) + recs F2/F4/F5/F6. **All F1–F6 fixed.** | [`FABLE_AUDIT.md`](FABLE_AUDIT.md) |
| **Security audit** | 2026-06-25 | `2597888` · v2.1.0-beta.10 · delta `906cf3d..HEAD` | High-risk surfaces + broad sweep; wordpress.org security bar | Claude (Opus 4.8) | **0 Critical / 0 High.** 2 Low (admin-gated SSRF on engine base_url; PII-at-rest cleartext), 2 Info (rate-limit transient; unconditional `error_log`). composer/npm deps clean. | [`SECURITY_AUDIT.md`](SECURITY_AUDIT.md) |
| **Code-quality + wp.org-readiness** | 2026-06-25 | `2597888` · v2.1.0-beta.10 · delta `906cf3d..HEAD` | Changed-code quality/architecture + full PCP 2.0.0 + plugin-review guidelines | Claude (Opus 4.8) | **No High defects; GA/upstream-ready.** **Punch-list APPLIED same day** (ABSPATH sweep, `error_log`→DebugLog, PCP `phpcs:ignore`s, blocks apiVersion 3, `.zipignore`); **`wp plugin check` against the built ZIP is clean** except 2 intentional (`Update URI`=fork guard → upstream merge; `(BETA)`=3.0 cut). ci:strict exit=0 (unit 374, JS 158). | [`CODE_QUALITY_AUDIT.md`](CODE_QUALITY_AUDIT.md) |
| **Upstream audit** (sendsmaily/smaily-wordpress-plugin) | 2026-06-03 | fork `39ade27` vs upstream `86da046` | Commit-by-commit review of the 14 upstream commits since fork-point | Claude Code | 0 security, 6 bug-fixes (**all cherry-picked** 2026-06-03), 3 compat open (#120/#128/#132) | [`UPSTREAM_AUDIT.md`](UPSTREAM_AUDIT.md) |
| **Upstream comparison** (snapshot) | 2026-06-12 | fork `bed2aa6` vs upstream tag `2.0.0` | Point-in-time feature/stack/version/quality comparison + version-collision story | Claude Code | Snapshot companion to the upstream audit; informs the merge strategy | [`UPSTREAM_COMPARISON.md`](UPSTREAM_COMPARISON.md) |
| **Mock ↔ real-engine divergence** | Phase 3.2.4 (living) | n/a (per-endpoint register) | Where the integration mock + payload design diverge from the deployed engine | Plugin side | Engineering tool — records divergences to close as the engine grows toward the contract | [`MOCK_DIVERGENCE_AUDIT.md`](MOCK_DIVERGENCE_AUDIT.md) |
| **3.1.0 release gate (F3-46)** | 2026-06-26 | `904f4ab` · v3.1.0 · delta = `LandingCapture` (new input/cookie surface) | Focused security pass on the new surface (read-only `$_GET` capture + first-party cookie write) + PCP against the BUILT ZIP | Claude (Opus 4.8) | **No new findings.** Input: `wp_unslash`+`sanitize_text_field`+strict regex (uuid / `vt_` / slug), no SQL/eval/file-IO, no echo (→ no XSS), no header-injection; nonce N/A (public read-only landing). Cookies: first-party, non-secret (rec_id uuid + opaque token), SameSite=Lax/Secure-on-https/HttpOnly=false (contract). No open redirect (redirect endpoint not built). **PCP on the ZIP clean** except the intentional `Update URI` (F3-35). ci:strict exit=0 (unit 391, JS 158); integration OK 119. | inline (this row) |
| **F3-47/F3-48 security re-audit** | 2026-06-30 | `c369486` · delta `522b305..HEAD` (contact-language resolver + contact-sync mode engine) | Independent security pass (fresh-context agent) on the delta: REST mode-write, new external HTTP (action-log/contact-list), the global `update_user_meta`/`add_user_meta` hooks, consent/PII, SQL | Claude (Opus 4.8, clean-context agent) | **0 Critical/High.** 1 Medium — reconcile→meta-hook echo could re-create a Smaily-`delete`d contact (GDPR hygiene) → **FIXED** (`ContactReconciler` reentrancy guard `is_applying()`/`run_suppressed()`; HookHandler skips); 1 Low (redundant consent re-POST, same root, fixed). Confirmed clean: REST `manage_options`-gated + mode validated; Authorization header never captured in `last_exchange`; action-log/contact PII never persisted; meta hooks no-DoS/no-recursion; consent (marketing vs profiling) separated; SQL `$wpdb->prepare`d. | [`2026-06-30-SECURITY_RE_AUDIT_F347_F348.md`](2026-06-30-SECURITY_RE_AUDIT_F347_F348.md) |
| **F3-47/F3-48 code-quality re-audit** | 2026-06-30 | `c369486` · delta `522b305..HEAD` | Independent correctness/quality pass (fresh-context agent) on the delta vs the design docs | Claude (Opus 4.8, clean-context agent) | 5 findings: **F1 Med — guest/checkout-opt-in sync inert** (preset 3 + `include_guests` synced nobody) → **FIXED** (order-path `should_sync_order_email` + classic+block checkout wiring); **F2 Med** = the security Medium (reconcile echo) → already fixed; **F4 Low** — settings-reducer mode coercion → **FIXED** (shared `normalizeContactSyncMode`); **F3 Low** (`list=1` semantics — resolved by `re/CONTACT_RECONCILIATION_DESIGN` §6.1; `rebaseline()` unwired) + **F5 Low** (new admin strings → `.pot`/`-et.po` at packaging) noted. | [`2026-06-30-CODE_QUALITY_RE_AUDIT_F347_F348.md`](2026-06-30-CODE_QUALITY_RE_AUDIT_F347_F348.md) |
| **3.2.0 release gate** | 2026-06-30 | `5034cc9` · v3.2.0 · delta `522b305..HEAD` (F3-47 + F3-48 + block-checkout attribution fix) | PCP against the BUILT ZIP, gated on top of the two re-audits above | Claude (Opus 4.8) | **PCP on the ZIP clean except the intentional `Update URI`** (F3-35); the 2 new `DynamicHooknameFound` warnings (prefixed filter constants `self::FILTER_*`) suppressed with a justified `phpcs:ignore`. ci:strict exit=0 (PHPUnit 456, vitest 161); integration OK 119; ET i18n complete. ZIP verified (build-hash `5034cc9`, v3.2.0, required present, dev-vendor absent). **Released: full GH release on the fork (`erkkimarkus/smaily-wordpress-plugin`), ZIP ~991 KB attached.** | inline (this row) |
| **3.2.1 release gate** | 2026-07-01 | `4b6fd3f` · v3.2.1 · delta `5034cc9..HEAD` (F3-48.5a contact-sync mode-selector UI refinement + doc-accuracy fixes + F3-48 live-walk) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (TSX-only admin-UI delta — no REST/auth/crypto/SQL/PII/external-HTTP surface, so the re-audit policy isn't triggered) | Claude (Opus 4.8) | **PCP on the ZIP clean except the intentional `Update URI`** (F3-35). ci:strict exit=0 (PHPUnit 456, vitest 161, tsc/eslint clean); ET i18n complete (new mode-hint string translated, admin catalog rebuilt). ZIP verified (build-hash `4b6fd3f` **clean**, v3.2.1, required present, dev-vendor absent). Gated by the **F3-48 Smaily contact-API live-walk 12/12 green** (sandbox `smailydemo`, `bin/walk-f3-48-contact-sync.cjs`). **Released: full GH release on the fork (`erkkimarkus/smaily-wordpress-plugin`), ZIP ~992 KB.** | inline (this row) |
| **3.3.0 / 3.3.1 / 3.3.2 release gates** (retroactive gap record) | 2026-07-03 (recorded 2026-07-07) | `16a530f` v3.3.0 · `8af6bc1` v3.3.1 (reverted by 3.3.2) · `a95b432` v3.3.2 | **No PCP re-run / security re-audit ran for these three tags** (recorded in STATUS at the time: small deltas — F3-49 beacon field, F3-50 consent advisory + revert — judged below the re-audit-policy threshold) and no register row was written, which itself violated the register discipline. | — (gap) | Security exposure retroactively closed: the 2026-07-07 T2 re-audit's delta (`4b6fd3f..8d22022`) explicitly swept the F3-49/F3-50 changes (clean). PCP coverage resumed at the v3.4.0 gate (row below). Lesson: a release gets a register row even when the policy says "no re-audit needed" — the row records that judgement. | inline (this row) |
| **T2 automations security re-audit** | 2026-07-07 | `8d22022` · delta `4b6fd3f..8d22022` (T2 automations proxy + UI; also sweeps the un-registered F3-49/F3-50 delta) | Security pass on the new admin REST surface (`AutomationsEndpoint` 3 routes incl. the PUT passthrough), the React render of engine-origin strings (XSS sinks), api_key leak paths, logging; + F3-49 visitor-token / F3-50 consent-advisory sweep | Claude (Fable 5) | **0 Critical/High/Medium.** 1 Low — engine-origin `docs` URL rendered as an unvalidated anchor href (`javascript:` scheme risk from a compromised engine) → **FIXED same pass** (`isHttpUrl()` guard, link renders only for http/https). 3 Info accepted (engine error text echoed as escaped text; no dedicated permission_check unit test — standard pattern; test_emails PII-light, never logged). Confirmed clean: capability+nonce on all 3 routes, no SSRF/injection via the PUT passthrough, api_key never reaches responses/logs/browser, 422 errors[] rendered text-only, F3-49/F3-50 escaped + whitelisted. | [`2026-07-07-SECURITY_RE_AUDIT_T2_AUTOMATIONS.md`](2026-07-07-SECURITY_RE_AUDIT_T2_AUTOMATIONS.md) |
| **3.4.0 release gate** | 2026-07-07 | `aa42ce6` · v3.4.0 · delta = T2 (contract v1.1.0 sync + automations proxy + settings UI + test-infra fix) | PCP against the BUILT ZIP, gated on the T2 security re-audit (row above) + the T2.3 live-walk (15/15 vs the real sandbox engine, `bin/walk-t2-automations.cjs`) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35); the old `mismatched_plugin_name` gone since GA. ci:strict exit=0 (PHPUnit unit 474, vitest 208, tsc/eslint clean); integration OK 126/594 (pre-walk); ZIP verified (clean build-hash `aa42ce6`, v3.4.0, required present, dev-vendor absent, ~1.05 MB). **Released: full GH release on the fork, tag `v3.4.0`, Latest.** | inline (this row) |
| **3.4.1 release gate** | 2026-07-07 | `ae3bc3d` · v3.4.1 · delta `aa42ce6..ae3bc3d` (T2.4 pilot-feedback UI fixes + contract v1.2.0 sync `recipe_en`) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (delta is React-admin-only + docs/mock/tests — shipped-PHP delta is the version-bump lines only, rest is the mock router + test files; no REST/auth/crypto/SQL/PII/external-HTTP surface, so the re-audit policy isn't triggered; per the 3.3.x lesson the row records that judgement) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35). ci:strict exit=0 (PHPUnit unit 474, PHPCS 0 errors, PHPStan clean, vitest 232, tsc/eslint clean); integration automations-suite OK 6 (full suite deliberately not run — PHP delta mock/test-only, sandbox connection preserved); ZIP verified (clean build-hash `ae3bc3d`, v3.4.1, required present incl. admin-bundle i18n JSON, dev-vendor absent, ~1.06 MB). **Released: full GH release on the fork, tag `v3.4.1`, Latest.** | inline (this row) |
| **3.4.2 release gate** | 2026-07-08 | `d6fb061` · v3.4.2 · delta `ae3bc3d..d6fb061` (F3-53: abandoned-cart poison-row hardening + legacy WP-Cron scheduler removal + `sync_subscribers` de-registration + abandoned-cart language via ContactLanguageResolver) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (delta ~350 lines in the LEGACY abandoned-cart/lifecycle path — removes code (scheduler, add_action) and adds defensive guards; no NEW REST/auth/crypto/SQL/PII-storage/external-HTTP surface — the resolver routing changes a value on an existing authenticated Smaily call; below the re-audit-policy threshold, judgement recorded per the 3.3.x lesson) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35). ci:strict exit=0 ×3 (PHPUnit unit 474, PHPCS 0 errors, PHPStan clean, vitest 232, tsc/eslint clean); **integration FULL suite OK 132/627** (+6 new F3-53 regression tests: poison shapes, Throwable backstop, resolver language on the wire, no legacy-cron re-arm, no `sync_subscribers` callback); dev-site sandbox connection snapshot/restored around both suite runs (verified `tenant=Smaily Connect test`). ZIP verified (clean build-hash `d6fb061`, v3.4.2, required present, dev-vendor absent, ~1.06 MB). **Released: full GH release on the fork, tag `v3.4.2`, Latest.** | inline (this row) |
| **3.4.3 release gate** | 2026-07-08 | `597ae8f` · v3.4.3 · delta `d6fb061..597ae8f` (F3-54: abandoned-cart status option normalizer + router-first dispatch + Settings array-shape writer + hydrate fix) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (delta ~630 lines, over half tests; the touched REST surface change is the SHAPE of one option write inside the existing authed `POST /settings` handler — no new route/auth/crypto/SQL/PII-storage/external-HTTP surface; below the re-audit-policy threshold, judgement recorded per the 3.3.x lesson) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35). ci:strict exit=0 (PHPUnit unit 480, PHPCS 0 errors, PHPStan clean, vitest 232, tsc/eslint clean; one pre-existing FLAKY vitest — `EngineAutomationsSection` "filters INACTIVE workflows", failed once under the parallel full run, passes in isolation ×2 and on rerun — noted, unrelated T2.4 surface); **integration FULL suite OK 137/652** (+5 `AbandonedCartSettingsSeamTest`: real-writer↔real-reader seam, corrupt `'1'`/`''` survive, router send from the mapping row, legacy-id fallback, id preserved + hydrate pin; +6 unit normalizer). Sandbox connection snapshot/restored (tenant verified). ZIP verified (clean build-hash `597ae8f`, v3.4.3, required present, dev-vendor absent, ~1.06 MB). **Released: full GH release on the fork, tag `v3.4.3`, Latest.** | inline (this row) |
| **PRO-1224/PRO-1230 security + code-quality re-audit** | 2026-07-10 | `fc7b577` · delta `ea5bce0..fc7b577` (PRO-1224 canonical `woo-<id>` identity + PRO-1230 hard-delete §3b catalog/remove + merchant docs site/links) | Policy-triggered (REST retry-kick change, NEW outbound HTTP `Client::catalog_remove()`, new `before_delete_post` enqueue surface, ~3.4k lines): security pass on every new surface + code-quality vs contract v1.3.0 §3/§3b + DECISIONS PRO-1224/1230 | Claude (Fable 5, clean-context agent) | **0 Critical/High/Medium.** 1 Low — cross-flusher tombstone/soft-delete ordering race when a variation delete and the parent remove enqueue in different requests (row stays `in_stock=false`; periodic re-sync reconciles) → **accepted, follow-up = ask the engine whether a plain upsert clears a tombstone's `recommendable=false`**. 4 Info accepted (older-engine 404 = terminal; dead guard; never-published `not_found` round-trip; per-row `sent_payload` convention). Confirmed clean: retry-kick capability-gated + fixed whitelist; Authorization never in stored exchanges; queue SQL fully prepared; event-type scoping; merchant SKU verifiably absent from every wire surface (data-minimizing); docs links `esc_url`-escaped. | [`2026-07-10-SECURITY_QUALITY_RE_AUDIT_PRO1224_PRO1230.md`](2026-07-10-SECURITY_QUALITY_RE_AUDIT_PRO1224_PRO1230.md) |
| **3.6.0 release gate** | 2026-07-10 | `f8903ce` · v3.6.0 · delta `ea5bce0..f8903ce` (PRO-1224 + PRO-1230 + docs site/links + version bump) | PCP against the BUILT ZIP, gated on the PRO-1224/1230 re-audit (row above) + the PRO-1224/1230 live-walk (LIVE OK, 20 checks, `bin/walk-pro1224-1230.cjs`, sandbox tenant) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35); one gate-time fix — 3.6.0 upgrade notice shortened under PCP's 300-char limit. ci:strict exit=0 (PHPUnit unit 500, vitest 236, PHPCS/PHPStan/tsc/eslint clean); integration not re-run (delta beyond the already-integration-green feature commits is version-bump + readme + i18n refs only; sandbox connection untouched). i18n rebuilt via `bin/build-i18n.sh` (new "Documentation" string in the `.mo`). ZIP verified (clean build-hash `f8903ce`, v3.6.0, required present, dev-vendor absent, ~1.07 MB). **Released 2026-07-10: full GH release on the fork, tag `v3.6.0`, Latest** — asset re-verified un-clobbered (1 075 310 B) after the expected harmless `release.yml` failure. MiuMjau's update rides the PRO-1233 engine-side purge + re-backfill (week of 2026-07-14). | inline (this row) |
| **3.6.1 release gate** | 2026-07-11 | `aa86c9a` · v3.6.1 · delta `f8903ce..aa86c9a` (PRO-1241 gross tax-inclusive order amounts + contract v1.4.0 sync + PRO-1240 dev-only `bin/` tooling + version bump) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (delta small: `OrderPayloadBuilder` amount-basis change + its unit/integration tests, contract doc sync, `bin/` snapshot tooling that does not ship in the ZIP — no new REST/auth/crypto/SQL/PII-storage/external-HTTP surface; below the re-audit-policy threshold, judgement recorded per the 3.3.x lesson). Gated on the PRO-1241 live-walk (LIVE OK 9/9, `bin/walk-pro1241-gross-orders.cjs`, sandbox tenant, invariant exact) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35). ci:strict exit=0 (PHPUnit unit 503, vitest 236, PHPCS/PHPStan/tsc/eslint clean); integration not re-run (delta past the integration-green feature commit `b249887` — 146 OK/721 — is version-bump + readme + `.pot` header only; sandbox connection verified intact, tenant `Smaily Connect test`). i18n rebuilt via `bin/build-i18n.sh` (no string changes — header refresh only; rebuild forced by the gate-time incident, see below). ZIP verified (clean build-hash `aa86c9a`, v3.6.1, required present, dev artifacts absent, 1 076 060 B). **Gate-time incident (recovered same session):** a container-side `rm -rf` on the bind-mounted `plugins/smaily-connect` path wiped the HOST working tree incl. `.git` mid-gate → full re-clone from `origin/main` (remote HEAD `b249887` had everything pushed; only the local version-bump commit was lost and was recreated), all builds + package + PCP + ci:strict re-run from scratch on the restored tree; CLAUDE.md PCP section now carries the never-touch-the-mount rule. **Released 2026-07-11: full GH release on the fork, tag `v3.6.1`, Latest** — asset re-verified un-clobbered (1 076 060 B, sha256 match) after the expected harmless `release.yml` failure. The historical MiuMjau net→gross order correction rides the engine-side PRO-1233 re-sync window (~2026-07-14), engine-coordinated — nothing merchant-side. | inline (this row) |
| **3.5.0 release gate** | 2026-07-08 | `ea5bce0` · v3.5.0 · delta `597ae8f..ea5bce0` (F3-55: backfill synced_count + audience estimate + UI copy) | PCP against the BUILT ZIP; **no** security/code-quality re-audit (delta ~650 lines, mostly tests + UI copy; new surface = two read-only fields on the existing authed `/backfill/status` + one COUNT query + migration 008 adding an INT column — no new route/auth/crypto/PII-storage/external-HTTP; below the re-audit-policy threshold, judgement recorded per the 3.3.x lesson) | Claude (Fable 5) | **PCP on the ZIP clean except the single intentional `Update URI`** (F3-35). ci:strict exit=0 (PHPUnit unit 480, PHPCS 0 errors, PHPStan clean, vitest 236, tsc/eslint clean; the `EngineAutomationsSection` dropdown flake recurred and was STABILIZED — the test now awaits the option, not the select); **integration FULL suite OK 139/674** (+2 `ContactBackfillAudienceTest`: SQL-count↔predicate agreement in every mode; real run reports walked/synced separately + REST payload). Sandbox connection snapshot/restored (tenant verified). i18n rebuilt (3 new strings, et complete, admin-bundle JSON verified). ZIP verified (clean build-hash `ea5bce0` + flake-fix `f92e4ef` test-only after, v3.5.0, required present incl. migration 008, dev-vendor absent, ~1.07 MB). **Released: full GH release on the fork, tag `v3.5.0`, Latest.** | inline (this row) |
| **PRO-1195 cart-rewrite security + code-quality re-audit (v3.7.0 gate)** | 2026-07-13 | `700d870` · delta `aa86c9a..HEAD` (v3.6.1 → pre-3.7.0 main; abandoned-cart rewrite PRO-1195 onto the namespaced pipeline + legacy-pass retirement, disabled-workflow dropdown filter (PRO-1277/1334), ProfilingConsent fail-open hardening (PRO-1194), uninstall.php `smly_profiling_*`/`smly_rec_*` sweeps (PRO-1336/1337), `/events` route test-pinning (PRO-1258), contract sync, new dev docs) | Policy-triggered (release boundary + >2,000 changed lines + new custom-table SQL/PII surface/uninstall sweeps/consent code): full delta read + security pass on every named high-risk surface (cart tracker SQL, checkout-input hooks, ProfilingConsent, uninstall, event-type queue scoping); confirmed untouched: REST route surface (beyond test-pinning), `/relay` beacon, auth/nonce, crypto | Claude (Fable 5, clean-context agent) | **PASS. 0 Critical/High/Medium/Low, 2 Info.** All `$wpdb` SQL on the new `smly_plus_cart_session` tracker + drain is `prepare()`d; checkout-input capture (classic + Store API) sanitized/validated before storage; cart-item capture is scalar-only (no free-text); PII retention unconditionally bounded (~24h, F3-37 backlog guard, runs even feature-off); event-type queue scoping holds both directions; F3-44 exchange capture never stores Authorization; F3-53 terminal-skip + no-legacy-WP-Cron-resurrection discipline holds and is directly tested; `uninstall.php`/`EnvScrub`/`SchemaMigrationTest` all correctly extended for the new table; zero new REST routes (grep-confirmed). 2 Info (both pre-existing/docs-scope, not delta regressions): a dead `uninstall.php` legacy-options entry that's actually a table-suffix string (no-op, harmless) — **fixed PRO-1342, 2026-07-17**; the new cart tracker's local PII isn't mentioned in `DATA_MODEL_GDPR.md` (out of that doc's stated rec-engine-only scope) or the privacy-policy template — closed by PRO-1343/PRO-1194 (see the 2026-07-16 delta-audit row below). No `ci:strict`/PCP/live-walk run in this pass (docs-only audit; STATUS.md already records green gates per commit) — those remain separate v3.7.0 release-gate steps. | [`2026-07-13-SECURITY_QUALITY_RE_AUDIT_PRO1195_CART_REWRITE.md`](2026-07-13-SECURITY_QUALITY_RE_AUDIT_PRO1195_CART_REWRITE.md) |
| **v3.7.0 gate delta re-audit (post-PRO-1195)** | 2026-07-17 | `bc5d6bc` · delta `af9b52f..HEAD` (GdprHandler/CartSessionStore WP Privacy coverage for the cart tracker PRO-1343 + friendly-name rename PRO-1405 + privacy-policy template sign-off/port PRO-1194, docs-only; PRO-1430 "How to add a Smaily signup form" guide — new `docsUrl` boot-payload field + admin React UI + docs-site HTML) | Policy-triggered (GDPR-surface change since the last audit's baseline): full delta read + security pass on the exporter/eraser SQL and data scoping, the new boot-payload field's render path, the new React clipboard-copy UI, docs-site HTML EN/ET parity | Claude (Fable 5, clean-context agent) | **PASS. 0 Critical/High/Medium/Low, 1 Info.** `CartSessionStore::rows_for_privacy_request()`/`delete_rows_for_privacy_request()` share one prepared-SQL WHERE builder so export and erase can never target different row sets; export drops the internal `id` + empty/null fields; friendly-name rename is string-only. `docsUrl` renders only as a JSX `href` (auto-escaped, `target=_blank rel=noopener noreferrer`), sourced from the existing filterable `Constants::docs_url()`, no new echo path. Clipboard-copy button copies a hardcoded constant only — no injection surface. Docs-site additions are static HTML, EN/ET siblings present, no external deps. Confirmed: this delta closes Info finding #2 from the 2026-07-13 audit (cart tracker now has real WP Privacy coverage + is documented in `DATA_MODEL_GDPR.md`/the privacy-policy template, now signed off and ported to `docs/site/index.html`); finding #1 was fixed separately under PRO-1342. 1 new Info (accepted, no action at current trust level): the `docsUrl` anchor has no scheme allowlist, unlike the T2-audit `isHttpUrl()` guard on an *engine-origin* URL — but this source is a developer-only PHP filter over a hardcoded https default, not third-party network input, so the stricter guard isn't warranted yet; revisit if `docs_url()` ever becomes admin-settings-writable. No `ci:strict`/PCP/live-walk run in this pass — STATUS.md already records green gates per code-touching commit (PRO-1343, PRO-1405, PRO-1430); those remain separate v3.7.0 release-gate steps. | [`2026-07-16-SECURITY_QUALITY_DELTA_AUDIT_V370_GATE.md`](2026-07-16-SECURITY_QUALITY_DELTA_AUDIT_V370_GATE.md) |
| **3.7.0 release gate** | 2026-07-17 | `39c606a` · v3.7.0 · delta `aa86c9a..39c606a` (PRO-1195 abandoned-cart rewrite, PRO-1277/PRO-1334 disabled-workflow dropdown filtering, PRO-1194 fail-open GDPR hardening, PRO-1336/PRO-1337 uninstall sweeps, PRO-1343/PRO-1405 WP Privacy cart-tracker coverage + rename, PRO-1430 signup-form guide, version bump) | PCP against the BUILT ZIP, gated on the two re-audits above (PRO-1195 cart-rewrite audit 2026-07-13 + the post-PRO-1195 delta re-audit 2026-07-17, both PASS) | Claude (Fable 5) | **PCP on the ZIP: 1 intentional ERROR (`plugin_updater_detected`, F3-35) + 4 new WARNINGs, all reviewed as false positives, no fix needed.** `CartAbandonmentSweeper.php:78` `DynamicHooknameFound` on `self::FILTER_MAX_AGE` — PCP can't resolve a class-constant reference to its literal value; the constant itself (`smaily_connect_abandoned_cart_max_age_seconds`) is correctly prefixed (same false-positive class as the 3.2.0 gate's `self::FILTER_*` findings, previously suppressed with `phpcs:ignore` — not done here, tracked as a follow-up). `CartSessionStore.php:83,302,321` `UnescapedDBParameter` (×3) on `$table`/`$sql` built with `{$this->table_name()}` — the table name is `$wpdb->prefix . self::TABLE_SUFFIX` (hardcoded internal constant, no user input); all interpolated *values* go through `$wpdb->prepare()` with `%s`/`%d` placeholders — standard safe pattern, matches the SQL already reviewed clean in the 2026-07-13 cart-rewrite audit. ci:strict exit=0 (PHPCS 0 errors, PHPStan clean, PHPUnit unit 570, vitest 244, tsc/eslint clean) run once after the version-bump commit. i18n rebuilt via `bin/build-i18n.sh` (PRO-1430 signup-guide strings; only `#:` reference-line refresh in `.pot`/`-et.po`, no msgid/msgstr change — Step5Integrations.tsx line shifts from the wording pass). Integration suite not re-run for this gate (delta beyond the already-integration-tested feature commits is version-bump/i18n-reference only; sandbox connection untouched, no docker exec beyond the read-only PCP container run). ZIP verified (clean build-hash `39c606a`, v3.7.0, required present incl. `dist/public/js/sc-runtime.js` + admin-bundle et JSON, dev artifacts/tests/docs/node_modules absent, 1 110 616 B). **Released: full GH release on the fork, tag `v3.7.0`, target `main`, Latest (non-prerelease); asset `smaily-connect.zip` byte-verified (1 110 616 B) matching the local build.** | inline (this row) |
| **v3.7.1 gate delta security audit (PRO-1390)** | 2026-07-17 | `a0a86ac` · delta `a2326df..HEAD` (v3.7.0 tag to now — PRO-1390 `/relay` cart_add/cart_remove sku fix + PRO-1342 `uninstall.php` cleanup + a live-walk record + a docs-wording correction) | Policy-triggered (change to the public, unauthenticated `/relay` route — named high-risk surface, independent of line count): security pass on the new `resolve_cart_product_skus()` server-side step (type coercion, injection, unbounded values, DoS via `wc_get_product` lookups, internal-field stripping, logging, abuse-filter interaction) + trivial quality pass on the rest | Claude (Fable 5) | **1 HIGH — release-blocking, RESULT: escalate.** `resolve_cart_product_skus()` (added by `674c04c`) runs a `wc_get_product()` lookup per `cart_add`/`cart_remove` event over the FULL raw `events` array **before** `validate_batch()`'s `MAX_EVENTS=100` cap is enforced, on the plugin's one public/unauthenticated route — a single unauthenticated POST with a large `events` array (well within default body-size limits; the per-IP/session rate limiter only throttles request count and does nothing on the first request) forces a large, unbounded number of DB-backed lookups before the batch is ever rejected as oversized. New DoS surface, did not exist pre-PRO-1390. **v3.7.1 should not be tagged until this is fixed** (reorder the size/shape cap ahead of the resolve loop) and re-verified. 0 Medium/Low/Info. Confirmed clean: `product_id` type-coerced safely (no crash/injection path), correctly stripped from the wire by the `EVENT_FIELDS` whitelist before reaching the engine (pinned by new unit + integration tests), the new drop-count log line carries no PII, `product_view`/other event types unaffected. The other 3 commits in the delta (PRO-1342 dead-entry removal, live-walk STATUS record, an audit-docs wording fix) are trivial/clean. **Finding 1 FIXED PRO-1446, `5ee1366` (2026-07-17):** `handle()` now runs the pure size cap (`size_guard()`, extracted from `validate_batch()`) on the raw array before `resolve_cart_product_skus()` — the resolve loop can never run over more than 100 events; new regression test proves zero `wc_get_product()` calls on a 101-event batch via a `woocommerce_product_class` filter counter. Valid-batch resolution behavior unchanged. ci:strict + full integration suite (157/157) green post-fix. **v3.7.1 may proceed.** | [`2026-07-17-SECURITY_DELTA_AUDIT_V371_GATE.md`](2026-07-17-SECURITY_DELTA_AUDIT_V371_GATE.md) |
| **3.7.1 release gate** | 2026-07-17 | `c25a4bb` · v3.7.1 · delta `a2326df..c25a4bb` (PRO-1390 browse cart_add/cart_remove sku fix, PRO-1446 /relay size-cap-before-resolve fix, PRO-1342 uninstall cleanup, version bump) | PCP against the BUILT ZIP, gated on the v3.7.1 delta security audit above (1 HIGH found and FIXED PRO-1446, independently re-verified) | Claude (Fable 5) | **PCP on the ZIP: exactly the expected set — 1 intentional ERROR (`plugin_updater_detected`, F3-35) + the same 4 PRO-1436 false-positive WARNINGs from the cart code (`CartAbandonmentSweeper.php:78` `DynamicHooknameFound`, `CartSessionStore.php:83,302,321` `UnescapedDBParameter` ×3), nothing new.** ci:strict exit=0 (PHPCS 0 errors, PHPStan no errors, PHPUnit unit 571/571, vitest 245/245, tsc/eslint clean) run after the version-bump commit. i18n rebuild SKIPPED (no `admin/src`/`languages` changes in the delta; `.mo`/admin-bundle JSON on disk still current from the 3.7.0 cut hours earlier). Integration suite not re-run for this gate specifically — the delta's `RecEngineBrowseProxyTest`/`BeaconEndpointTest` changes were already integration-proven the same day at the PRO-1446 fix commit (157/157, sandbox tenant restored). ZIP verified (v3.7.1 in all three version strings, required present incl. `dist/public/js/sc-runtime.js` + admin-bundle et JSON, dev artifacts/tests/docs/node_modules absent, 1 114 299 B vs. v3.7.0's 1 110 616 B). **Released: full GH release on the fork, tag `v3.7.1`, target `main`, Latest (non-prerelease); asset `smaily-connect.zip` byte-verified (1 114 299 B) matching the local build.** | inline (this row) |
| **v3.7.2 gate delta security audit (PRO-1388)** | 2026-07-21 | `5f9b031` · delta `f470faf..HEAD` (v3.7.1 release record to the pre-bump tip — PRO-1388 browser-side consent-independent attribution capture + its tests, PRO-1447 contract sync to v1.5.0, PRO-1436 PCP-suppression comments) | Policy-triggered (change to the consent surface — `beacon-core.ts`/`rec-engine-client.ts`, a named high-risk surface, independent of line count): security pass on whether `captureUrlParams()` still writes only the three attribution cookies and never a session or a send pre-consent, any new injection/XSS surface in the URL-param handling, cookie flags; trivial read on the doc/comment-only commits | Claude (Fable 5) | **PASS. 0 Critical/High/Medium/Low/Info.** `captureUrlParams()` (now called unconditionally from `init()`, PRO-1388's `6d8bd6d`) writes only the three attribution cookies via the unchanged `setCookie()` helper (`encodeURIComponent`-escaped values, `Path=/`, `SameSite=Lax`, conditional `Secure`, no `HttpOnly` — correct for a cookie the same JS must read back) and contains no session-cookie creation or network call; `ensureSession()`/`track()`/`flush()`/cart-listener attachment stay inside the still-consent-gated `start()`. URL-param parsing uses `URLSearchParams` (no manual string parsing/`eval`/DOM-injection) and the cookie-write/URL-rewrite logic is byte-identical to before the consent-gate removal, so no new injection surface was introduced. New vitest cases (`beacon-core.test.ts`, `rec-engine-client.test.ts`) pin exactly this: cookie written + URL stripped + zero fetch calls pre-consent, and a pre-consent-captured token correctly riding a later, still consent-gated send. `ea76f8d` (contract sync) confirmed additive-only via `git diff --stat`; `5f9b031` (PCP suppressions) confirmed comment-only. **v3.7.2 may proceed.** | [`2026-07-21-SECURITY_DELTA_AUDIT_V372_GATE.md`](2026-07-21-SECURITY_DELTA_AUDIT_V372_GATE.md) |
| **3.7.2 release gate** | 2026-07-21 | `bc7bcc9` · v3.7.2 · delta `f470faf..bc7bcc9` (PRO-1388 consent-independent browser-side attribution capture + tests, PRO-1447 contract sync, PRO-1436 PCP-suppression comments, version bump) | PCP against the BUILT ZIP, gated on the v3.7.2 delta security audit above (clean, no findings) | Claude (Fable 5) | **PCP on the ZIP: exactly the expected set — 1 intentional ERROR (`plugin_updater_detected`, F3-35), nothing else.** The `due_rows()` method in `CartSessionStore.php` (same `{$this->table_name()}` interpolation pattern as the sites suppressed by PRO-1436) was specifically checked and did NOT get flagged — no new PCP finding of any kind surfaced. ci:strict exit=0 (PHPCS 0 errors, PHPStan clean, PHPUnit unit 571/571, vitest 248/248, tsc/eslint clean) run after the version-bump commit. i18n rebuild SKIPPED (no `admin/src`/`.po` changes since v3.7.1; on-disk `.mo`/admin-bundle JSON confirmed still current). Integration suite re-run in full: `sg docker -c "composer run test:integration"` OK (157 tests, 791 assertions), sandbox tenant "Smaily Connect test" correctly restored post-run (not MiuMjau). ZIP verified (v3.7.2 in all three version strings, required present incl. `dist/public/js/sc-runtime.js`/`dist/admin/admin.js`/`blocks/*/build/*`/`vendor/autoload.php`/`languages/smaily-connect-et.mo`, dev artifacts/tests/docs/node_modules/admin-src absent, 1 116 466 B). **Gates green — publication (GH release + tag) deferred to the orchestrator in this session; not performed by this pass.** | inline (this row) |
| **v3.8.0 gate delta security audit (PRO-1389)** | 2026-07-21 | `04789b8` · delta `bc7bcc9..04789b8` (v3.7.2 bump to the pre-3.8.0-bump tip — PRO-1389 ongoing-session identity injection on `/relay` + tests + dedup follow-up, PRO-1402 decision record, docs refresh, PRO-1491 catalog default-category fallback + auto-draft-skip fixes + mock strictness) | Policy-triggered (auth/capability + consent logic change on the public, unauthenticated `/relay` route — a named high-risk surface, independent of line count): adversarial security pass on `attach_logged_in_identity()`/`resolve_logged_in_email()` — does injection fire only for a validated logged-in cookie, can a forged/expired cookie cause injection, does the opted-out path forward unchanged, is the email absent from every response/error path, any timing/enumeration angle via the consent lookup; targeted check on the PRO-1491 catalog commits for any new user-input surface; trivial read on the doc-only commits | Claude (Fable 5) | **PASS. 0 Critical/High/Medium/Low/Info.** `resolve_logged_in_email()` gates exclusively on WordPress core's own `wp_validate_auth_cookie( '', 'logged_in' )` — a forged cookie (no knowledge of the site's `AUTH_KEY`/`LOGGED_IN_KEY` secrets) or an expired one both fail core's own HMAC/expiry check and resolve to `''` (treated as anonymous, never an error); the endpoint's pre-existing lack of a nonce is unchanged by this delta and only matters for request-forgery (riding the caller's OWN cookie), which cannot attach a victim's identity. The opted-out path returns the events array byte-for-byte unmodified (no `customer_email` added) — forwarded anonymous, never dropped. None of the six response shapes `handle()` can return carries `customer_email`; it exists only in the outbound engine request. The `895c7da` consent-lookup dedup only skips a redundant `may_profile()` call for the server-resolved (not attacker-chosen) email, so it narrows rather than widens any timing surface; a pre-existing, unrelated Info-level observation is noted (an unauthenticated caller could already probe `may_profile()` via a self-supplied `customer_email` field, unchanged since the 3.4.0-era beacon and the (a).1 gate, constrained by rate limiting) — explicitly out of scope for this delta, not a regression. 8 unit tests (`BeaconEndpointIdentityTest`) + 5 integration tests (`RecEngineBrowseProxyTest`, real `logged_in`-cookie path via `set_logged_in_cookie`) directly pin every angle checked. PRO-1491's catalog commits read only internal WP options/terms, take no new user input, and open no new surface — confirmed by reading the diffs directly, not assumed. **v3.8.0 may proceed.** | [`2026-07-21-SECURITY_DELTA_AUDIT_V380_GATE.md`](2026-07-21-SECURITY_DELTA_AUDIT_V380_GATE.md) |
| **3.8.0 release gate** | 2026-07-21 | `9cd41be` · v3.8.0 · delta `bc7bcc9..9cd41be` (PRO-1389 ongoing-session identity injection + tests + dedup, PRO-1491 catalog default-category fallback + auto-draft-skip fixes + mock strictness, PRO-1402 decision record, docs refresh, version bump, gate-time upgrade-notice-length fix) | PCP against the BUILT ZIP, gated on the v3.8.0 delta security audit above (clean, no findings) | Claude (Fable 5) | **PCP on the ZIP: 1 intentional ERROR (`plugin_updater_detected`, F3-35) — the first PCP run also caught a real `upgrade_notice_limit` WARNING on the 3.8.0 upgrade notice (368 chars, over PCP's 300 limit); fixed same-session (`9cd41be`, content unchanged, just tighter wording — same class of fix as the v3.6.0 gate) and the ZIP rebuilt; the re-run PCP is exactly the expected set, nothing else.** ci:strict exit=0 (PHPCS 0 errors, PHPStan clean, PHPUnit unit 585/585, vitest 248/248, tsc/eslint clean) run after the version-bump commit. i18n rebuild SKIPPED (no `admin/src`/`languages` changes since v3.7.2, confirmed via `git log` over the full delta). Integration suite re-run in full: `sg docker -c "composer run test:integration"` OK (162 tests, 814 assertions), sandbox tenant "Smaily Connect test" correctly restored post-run (not MiuMjau). ZIP verified (v3.8.0 in all three version strings, required present incl. `dist/public/js/sc-runtime.js`/`dist/admin/admin.js`/`blocks/*/build/*`/`vendor/autoload.php`/`languages/smaily-connect-et.mo`+ admin-bundle et JSON, dev artifacts/tests/docs/node_modules/admin-src absent, 1 119 339 B vs. v3.7.2's 1 116 466 B). **Gates green — publication (GH release + tag) deferred to the orchestrator in this session; not performed by this pass.** | inline (this row) |
| **PRO-1486 register note (`/relay` customer_email strip)** | 2026-07-21 | `HEAD` (post-v3.8.0 — `BeaconEndpoint::EVENT_FIELDS` no longer accepts a client-supplied `customer_email`; `filter_by_profiling()` dead-branch simplified) | Narrowing-only change to the already-audited `/relay` public route (closes the exact Info-level observation the v3.8.0 delta audit above recorded: "an unauthenticated caller could already probe `may_profile()` via a self-supplied `customer_email` field"). Per re-audit policy this is a REGISTER NOTE, not a full re-audit: the change only REMOVES an accepted input (no new route, no new external HTTP, no new SQL, no new auth/crypto surface) and is strictly narrower than the surface the v3.8.0 audit already reviewed | Claude (Fable 5) | **No new finding — this change CLOSES one.** The change makes `customer_email` unspoofable from the client (whitelist removal, not a runtime check that could itself be buggy); the sole remaining source (`attach_logged_in_identity()`, PRO-1389) and its consent gate (`filter_by_profiling()`) are unchanged in effect (one dead per-event branch simplified, loop/counter/logging kept as defense-in-depth). ci:strict exit=0 (PHPUnit unit 595/595, vitest 248/248, PHPCS/PHPStan/tsc/eslint clean); `sg docker -c "composer run test:integration"` OK (163 tests, 823 assertions), sandbox tenant "Smaily Connect test" restored post-run. Full re-audit deferred to the next release-gate pass (per policy, a narrowing change doesn't trigger one on its own). | [`docs/DECISIONS.md` PRO-1486 entry](../DECISIONS.md) |
| **3.8.1 release gate** | 2026-07-21 | `251a29f` · v3.8.1 · delta `9cd41be..251a29f` (PRO-1498 catalog tombstone force-fill so a deleted/unresolvable product is never silently skipped, PRO-1486 `/relay` customer_email strip — full adversarial pass promised by the register note above, PRO-1499 contract v1.6.0 sync + `tags.category_defaulted`, version bump) | Full delta security audit (release boundary, policy point 1) — adversarial focus on the `/relay` strip (nested/array bypass check, injection-ordering check) per the register note's deferral, plus a new-input-surface check on the PRO-1498/1499 catalog commits; PCP against the BUILT ZIP | Claude (Fable 5) | **0 Critical/High/Medium/Low/Info.** `/relay` strip confirmed a pure narrowing: the whitelist copy is a flat top-level exact-key copy (no merge/deep-scan), so `customer_email` can't be smuggled back in via case, nesting under an allowed field, or type confusion; injection ordering (`validate_batch` strip → `attach_logged_in_identity` → `filter_by_profiling`) unchanged, only dead differing-email-branch removed. PRO-1498/1499 catalog commits add no new route/external HTTP/SQL/input surface — operate only on internal WP post ids + already-built catalog objects; synthetic fallback URL built from a cast `int`, no injectable concatenation. **PCP on the ZIP: exactly the 1 expected intentional finding** (`plugin_updater_detected`, F3-35) — no `upgrade_notice_limit` warning this time (267 chars). ci:strict exit=0 (PHPCS 0 errors, PHPStan clean, PHPUnit unit 595/595, vitest 248/248, tsc/eslint clean). i18n rebuild SKIPPED (no `admin/src`/`languages` changes since v3.8.0). `sg docker -c "composer run test:integration"` OK (163 tests, 826 assertions), sandbox tenant "Smaily Connect test" correctly restored post-run (not MiuMjau). ZIP verified (v3.8.1 in all three version strings, required present, dev artifacts/tests/docs/node_modules/admin-src absent, 1 122 412 B vs. v3.8.0's 1 119 339 B). **Gates green — publication (GH release + tag) deferred to the orchestrator in this session; not performed by this pass.** | [`2026-07-21-SECURITY_DELTA_AUDIT_V381_GATE.md`](2026-07-21-SECURITY_DELTA_AUDIT_V381_GATE.md) |
| **v3.9.0 gate delta security audit (PRO-1504/1518/1519/1506/1517 + Update URI removal)** | 2026-07-23 | `69a83b8` (pre-cut main tip) · delta `123d479..69a83b8` (v3.8.1-publication tip to the pre-v3.9.0-bump tip — PRO-1504 Stages 1+2: transactional-email settings, mapping UI, sender (`Client::send_message()`), gate, suppression, fail-open; PRO-1518 Store-API/block-checkout order-confirmation twin; PRO-1519 bounded retry ceiling; PRO-1506 flush-time catalog-tombstone repair; PRO-1517 mock-only GDPR-fidelity fixes; F3-35 `Update URI` header removal) | Policy-triggered on BOTH grounds (release boundary + >2,000 changed lines, 5255/153 across 46 files): full delta read + adversarial pass on the brief's six focus areas — transactional credential storage/logging, send/gate/suppression/fail-open double-send and forged-trigger risk, the REST/settings surface, the Store-API twin's idempotence, the PRO-1519 ceiling's boundedness, the PRO-1506 placeholder-URL construction, PRO-1517's shipped-tree scope, and the Update URI removal's blast radius | Claude (Fable 5, clean-context agent) | **0 Blocker/Critical/High. 1 Should-fix (Medium), 0 Low, 2 Info.** Should-fix: `TransactionalPayloadBuilder`'s `context` merge-tag fields (`first_name`, `last_name`, `order_number`, `payment_method`, `shipping_method`, `product_name`, `product_description`) are sent to `message/send.php` WITHOUT the `htmlspecialchars()` treatment the sibling `CartPayloadBuilder` explicitly applies to the same-purpose fields for the same delivery mechanism ("ready to drop straight into an email merge tag") — `first_name`/`last_name` are attacker-controlled via ordinary WC checkout input and the recipient (`billing_email`) is also attacker-chosen, so this is a real content-injection path into a transactional email IF Smaily's `message/send.php` merge-tag substitution doesn't itself escape (unverified external behavior, not proven either way) — not a release blocker (default-off feature, narrow mechanical fix, no proven local vulnerability), recommended as a fast-follow before pilot rollout. Confirmed clean elsewhere: credential storage reuses the existing `encrypt_password()`/`Credentials` path with zero new crypto surface and the password is never echoed in the boot payload; `Client::send_message()`/`TransactionalFlusher::record_exchange()` never capture the Authorization header (directly tested); the gate is genuinely default-off (`get_option(..., false)`, tested) and all three new WC hook bindings are internal, unauthenticated-unreachable, and register unconditionally SAFELY because the gate self-gates every attempt; fail-open and the once-per-order-per-type meta guard make a double-send structurally impossible across sync+retry and across the PRO-1518 twin hooks (directly tested); the PRO-1519 ceiling is type-scoped and time-bounded (1 hour), reusing the existing terminal-failure path with no new fallback logic; no new REST route (existing `/settings` route, same capability+nonce), and the new automation-mapping `trigger_type` allowlist genuinely closes the "any string reaches an INSERT" gap named in the Stage 1 STATUS record; PRO-1506 reuses two PRE-EXISTING, already-audited `CatalogPayloadBuilder` methods at a new call site (the builder file itself is untouched in this delta); PRO-1517 touches only `tests/`/`docs/` (confirmed via `git show --stat`), nothing shipped; the `Update URI` removal is header/comment-only (13 lines) and correctly retires the long-carried intentional PCP `plugin_updater_detected` finding — the v3.9.0 PCP run should come back with ZERO findings, and a reappearance of that finding should read as a regression, not the old accepted baseline. PCP against the built ZIP explicitly deferred to the release-build worker later today (out of scope for this pass per the task brief). No `ci:strict`/integration re-run in this pass (docs-only audit; STATUS.md already records green gates per code-touching commit). **Should-fix FIXED same-day, PRO-1537:** all seven fields now `htmlspecialchars()`-escaped (mirrors `CartPayloadBuilder`'s exact idiom), adversarial unit tests added, `ci:strict` exit=0 (PHPUnit unit 652/652, +2). | [`SECURITY_DELTA_2026-07-23.md`](SECURITY_DELTA_2026-07-23.md) |
| **Pre-v3.10.0 gate delta security audit (PRO-1540/1539/1538/1534)** | 2026-07-23 | `0bf43df` · delta `69a83b8..0bf43df` (the v3.9.0-gate audit's own baseline tip to the pre-v3.10.0-cut tip — PRO-1540 `SettingsEndpoint` transactional-account/-triggers handler split + wizard UI relocation, PRO-1539 EventLog aged-failure bulk-retry control, PRO-1538 PCP `meta_key` suppression comments, PRO-1534 contract-doc/mock sync, PRO-1537 escaping fix landing in this same delta) | Policy-triggered (release boundary, v3.10.0 cut) | Claude (Fable 5) | **0 Blocker/Critical/High/Medium/Low. 0 new findings.** PRO-1540's handler split preserves capability/nonce/sanitization/credential-persistence byte-for-byte (same route, same `permission_check()`, same `persist_credentials()`, same `sanitize_key()` allowlist on `shippedOrderStatuses`) — confirmed by direct diff read + the split `SettingsEndpointTest` cases proving each tab writes only its own slice. PRO-1539's new "Retry all failed" button calls the pre-existing `retryEvents()` → `/events/retry` route, no new endpoint. PRO-1538 is comment-only (2 `phpcs:ignore` lines). PRO-1534 is a doc/mock-only field-name correction, no plugin code reads/writes the removed field. PRO-1537 (already this file's Should-fix, resolved same-day) re-confirmed unchanged. No `ci:strict`/integration re-run in this pass (docs-only audit; STATUS.md records green gates per commit). PCP against the built ZIP deferred to the release-build worker. | [`SECURITY_DELTA_2026-07-23.md`](SECURITY_DELTA_2026-07-23.md) (Addendum section) |
| **v3.11.0 gate delta security audit (upstream merge + the PRO-16xx/17xx defect sweep)** | 2026-08-07 | `5b3dbbc` (pre-cut main tip) · delta `36977de..5b3dbbc` (the v3.10.0 tag to the pre-cut tip — **100 commits, 157 files, +12 080 / −2 166**: the sendsmaily upstream merge `4a3d979`, plus PRO-1679 block-checkout first-order, PRO-1680/1729 abandoned-cart fields, PRO-1685 Smaily retry policy + migration 010, PRO-1681 automation markers, PRO-1683/1684/1743/1772 field-selection interpreter, PRO-1682 welcome-trigger narrowing, PRO-1742 contact-sync switch, PRO-1716 force-opt-in retirement, PRO-1715/1769 contact backfill, PRO-1686 refusal classification, PRO-1710 rec-id UUID validation, PRO-1633 return signals, PRO-1767 standalone attribution bundle, PRO-1712 `/relay` whitelist narrowing, contract syncs v1.6.0 → v1.8.1) | Policy-triggered on BOTH grounds (release boundary + far over the >2,000-line threshold), and on almost every named high-risk surface: the public `/relay` route, a new storefront JS bundle writing cookies pre-consent, custom-table SQL (migration 010, `EventQueue`, `BackfillJob`), REST admin routes, consent/GDPR posture, and what gets stored/logged. Full file-by-file read of the production delta + adversarial pass on the six focus surfaces named in the release brief | Claude (Fable 5) | **0 Blocking/Critical/High. 1 Medium (should-fix, non-blocking), 1 Low, 4 Info.** Medium: `public/js/lib/attribution.ts` applies the UUID check to `smaily_rec` (PRO-1710, correctly ported) but **not** the `vt_…` / slug shape+length checks its PHP twin `LandingCapture::resolve()` applies to `smaily_vt` / `smaily_ctx` — pre-existing on browse-enabled stores, but PRO-1767's `sc-landing.js` newly gives that permissive writer to browse-OFF stores whose only writer used to be the strict PHP one; a crafted landing URL can plant an arbitrary/oversized value in a 30/365-day cookie that rides to order meta (`sanitize_text_field`, no cap) and onto the §5 orders wire. Not a privilege boundary and no data exposure → fast-follow, not a blocker. Low: the retired `smly_plus_contact_sync_automation_force_opt_in` option is left on disk unread (direction is safe — `force_opt_in` is now hard-`false` everywhere, GDPR Art. 21). Confirmed clean elsewhere: PRO-1712 is a pure whitelist narrowing on the only public route (exact-key copy loop unchanged, no case/alias/nesting bypass, `attach_logged_in_identity()` untouched); `sc-landing.js` is 1 194 bytes with **zero** `fetch`/`XHR`/`sendBeacon`/top-level `import` and its boot blob carries no `beaconUrl`/tenant/key, and it is mutually exclusive with the runtime; all new/rewritten SQL is correctly parameterised (migration 010 is dbDelta-shaped; `%d` inside `INTERVAL`; UTC on both sides of the retry comparison) and three legacy statements that mis-`prepare()`d a table name were corrected by the upstream merge; `RetryPolicy` clamps a Smaily-supplied `Retry-After` at 6 h and ends the pre-existing infinite 60 s re-POST of permanent 4xx refusals; PRO-1716/1682/1742 all move consent handling in the safer direction; REST permission callbacks unchanged (all admin routes on `Constants::CAPABILITY`); `TestConnectionEndpoint` echoes no part of the upstream body; `bin/exchange-setup-token.php` has ABSPATH + CLI guards and is `.zipignore`d. Gates for this pass: `npm run ci:strict` exit=0 (PHPUnit unit 739/739, vitest 275/275, PHPStan `[OK]`, PHPCS 0 errors). No product code changed by the audit. PCP against the built ZIP deferred to the release-build pass. | [`SECURITY_DELTA_AUDIT_2026-08-07.md`](SECURITY_DELTA_AUDIT_2026-08-07.md) |
| **3.11.0 release gate** | 2026-08-07 | `ed49abc` · v3.11.0 · delta `36977de..ed49abc` (the v3.10.0 tag to the cut: the 100-commit upstream-merge + PRO-16xx/17xx sweep enumerated in the row above, plus bump `c73b7b9` and i18n restamp `ed49abc`) | PCP against the BUILT ZIP, gated on the v3.11.0 delta security audit above (0 Blocking/Critical/High) | Claude (Fable 5) | **PCP on the ZIP: 0 ERRORS, 5 WARNINGs — the first fully error-free PCP release gate, and the NEW BASELINE future gates compare against.** The `plugin_updater_detected` ERROR every gate from 3.2.0 onward carried is confirmed GONE (the `Update URI` header was removed in v3.9.0 ahead of the upstream merge, F3-35) — a future gate reporting any ERROR is a regression, not "the usual one". Four warnings are the known accepted set: `BackfillJob.php:513` `DirectDatabaseQuery.DirectQuery` + `.NoCaching` (custom-table query, the W-6 class — prepared, Security-audit-verified) and `HookHandler.php:231` `DynamicHooknameFound` on `self::FILTER_WELCOME_ELIGIBLE` (the documented false-positive class — PCP can't resolve a class constant to its correctly-prefixed literal; same as the 3.2.0 and 3.7.0 gates). The fifth is real but **deliberately shipped as-is**: `upgrade_notice_limit` — the 3.11.0 upgrade notice is 636 chars against PCP's 300. Earlier gates (3.6.0, 3.8.0) tightened the wording at the cut; Erkki's call here is to leave it and fold it into the **wordpress.org readme-prep** item together with the changelog trim (`readme_parser_warnings_trimmed_section_changelog` — the Changelog section is 32 927 chars against the wp.org parser's 5 000 limit, so everything below ~3.10.0 would be silently truncated on a wp.org listing). Neither affects a GH-fork release; both must be resolved before the upstream/wordpress.org submission (PR #135). **`ci:strict` exit=0** (PHPCS 0 errors, PHPStan `[OK] No errors`, PHPUnit unit 739/739, vitest 275/275 across 37 files, tsc/eslint clean). **Integration suite RE-RUN in full**: `sg docker -c "composer run test:integration"` **OK (254 tests, 1485 assertions)**, 1 pre-existing environment skip (`BackwardCompatTest` — the legacy double-sync premise doesn't apply in this env), dev sandbox tenant "Smaily Connect test" correctly restored post-run (not MiuMjau, `connected=1`). i18n rebuild RUN (`bin/build-i18n.sh`). ZIP verified (v3.11.0 in the plugin header + both version constants + readme Stable tag, clean non-dirty build-hash `ed49abc`; required present incl. **both** storefront bundles `dist/public/js/sc-runtime.js` and the PRO-1767 `dist/public/js/sc-landing.js`, all three `blocks/*/build/*`, `vendor/autoload.php`, `languages/smaily-connect-et.mo` + admin-bundle et JSON, `composer.json`; tests/docs/node_modules/admin-src/`dist/client`/dev-vendor absent, the only shipped vendor package being `woocommerce/action-scheduler`; 401 files, **1 191 528 B** vs. v3.10.0's 1 148 878 B). **Released: full GH release on the fork, tag `v3.11.0` on `ed49abc79767854487cfb04a14df412d99c02942` — HEAD, not the bump commit, so the tagged tree matches the ZIP's embedded build-hash — target `main`, Latest (non-prerelease); asset `smaily-connect.zip`, SHA256 `a8c1c6800fb0f1dcf08394e09b54b73df87a9e73933540887f2912b136ba449d`.** | inline (this row) |
| **3.11.1 release gate** | 2026-08-10 | `e6413e5` · v3.11.1 · delta `ed49abc..HEAD` (PRO-1878 capture side — immediate `checkout_complete` flush; PRO-1896 attribution hardening — browser writer shape checks + order-meta length cap; PRO-1897 retired force-opt-in option deleted on upgrade; PRO-1900 wordpress.org readme prep; docs hygiene; bump `e6413e5`) | PCP against the BUILT ZIP; **no** new security/code-quality re-audit — the delta is 9 commits / ~250 changed plugin lines (far under the ~2 000-line rule of thumb) and its only security-sensitive content **is** the fast-follow the 2026-08-07 v3.11.0 delta security audit itself prescribed (its 1 Medium → PRO-1896, its 1 Low → PRO-1897); no REST route, `/relay` field set, capability, crypto, SQL, secret/PII or consent surface changed | Claude (Fable 5) | **PCP on the ZIP: 0 ERRORS, 3 WARNINGS — down from the 3.11.0 baseline's 5, with both readme warnings resolved.** PRO-1900 fixed them: `upgrade_notice_limit` (the 3.11.0 notice re-worded from 636 to 298 bytes against PCP's `strlen` limit of 300; the new 3.11.1 notice is 289) and `readme_parser_warnings_trimmed_section_changelog` (the Changelog now carries the 3.x line only, with the 1.x/2.x history linked to the GitHub releases page). **Correction to the 3.11.0 row above:** that limit is 5 000 **words**, not characters (`Lib/Readme/Parser.php` `maximum_field_lengths['section-changelog']`, `trim_length(…, 'words')`) — the section was ~5 077 words, only just over; it is now ~3 300 with ~35% headroom. The 3 remaining warnings are exactly the known accepted set (`BackfillJob.php:513` `DirectDatabaseQuery.DirectQuery` + `.NoCaching`; `HookHandler.php:251` `DynamicHooknameFound` on `self::FILTER_WELCOME_ELIGIBLE`). **`ci:strict` exit=0** (PHPCS 0 errors, PHPStan `[OK] No errors`, PHPUnit unit 741/741, vitest 282/282 across 37 files, tsc/eslint clean). **Integration suite re-run in full**: `sg docker -c "composer run test:integration"` **OK (256 tests, 1491 assertions)**, 1 pre-existing environment skip (`BackwardCompatTest`), dev sandbox tenant "Smaily Connect test" correctly restored post-run (not MiuMjau, `connected=1`). i18n rebuild SKIPPED (no `languages/` or `admin/src/` change since `ed49abc`, and the `includes/` delta adds no translatable string). ZIP verified (3.11.1 in the plugin header + both constants + readme Stable tag, clean non-dirty build-hash `e6413e5`; both storefront bundles, all three `blocks/*/build/*`, `vendor/autoload.php`, `languages/smaily-connect-et.mo` + admin-bundle et JSON, `composer.json` present; tests/docs/node_modules/`admin/src`/`dist/client`/dev-vendor absent; 400 files, **1 191 727 B** vs. v3.11.0's 1 191 528 B). **Gates green — publication (tag + GH release) deferred to the orchestrator; not performed by this pass.** | inline (this row) |

---

## Re-audit policy — NOT optional

An audit is a snapshot of a specific repo state. Code moves; a stale audit hands
false confidence (the 2026-06-25 audits exist precisely because ~10k lines landed
after the 2026-06-11 one). So:

**Re-run the security + code-quality audits after any of these:**
1. **A release boundary** — before cutting a GA / non-beta tag, and before any
   **upstream/wordpress.org submission** (the public review bar is higher).
2. **A large delta** — rule of thumb: **> ~2,000 changed lines** of plugin code,
   or any change touching a **security-sensitive surface**: a new/changed REST
   route, the public `/relay` beacon, auth/capability/nonce logic, crypto, SQL
   against custom tables, what gets stored/logged (secrets/PII), GDPR/consent, or
   file/HTTP I/O with external input.
3. **A new external trust boundary** — a new outbound destination, a new
   data-at-rest surface, or a new public/unauthenticated entry point.

**Scope of a re-audit** = the delta since the last audit's baseline (its
`Repo state` column), plus a security pass on any high-risk surface the delta
touched, plus PCP **against the built ZIP** for a release/submission gate. Record
the new run as a row above + a dated report, and bump the relevant doc baselines.

**Where this is enforced:** `CLAUDE.md` (operational rhythm) points here; `STATUS.md`
records each run. If you make a bigger change and skip the re-audit, that's a defect
— treat it like a skipped gate.
