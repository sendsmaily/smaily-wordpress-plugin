# Changelog

This file is no longer maintained. Since 3.13.0 the changelog lives in two
places that are always current:

- **`readme.txt`** — the `== Changelog ==` section, the changelog merchants and
  wordpress.org read.
- **GitHub releases** — <https://github.com/sendsmaily/smaily-wordpress-plugin/releases>,
  carrying the same notes per tag.

The file is kept so existing links resolve. Its old content (up to 3.5.0) is in
the git history.
